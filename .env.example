# Optional: point the UI at a non-default backend origin.
# Dev default is same-origin with Vite proxying /api + /actuator to the Azure backend.
# VITE_API_URL=https://stockintel-api-new-b7bafzh6hhb9edhq.centralindia-01.azurewebsites.net
